# sap-harness followup contract

This is a target-repository implementation sequence, not a claim that the skills change has migrated sap-harness. File/line revalidation and the inspected target revision are recorded in skills#267. Revalidate those files before applying the followup; credential contents and live SAP operations are unnecessary for these offline checks.

| Order | Target-owned change | Completion evidence |
|---|---|---|
| 1 | Extract the required preparation body from `.claude/hooks/worktree-setup.sh`; propagate npm installation and other required failures as nonzero. Keep package/workspace identity checks. | A failed install leaves no ready state, preserves the worktree and succeeds after an explicit retry; a trailing success exit cannot mask failure. |
| 2 | Make `.claude/skills/live-capture/preflight.mjs` consume the actual CLI/vault resolution contract from `packages/cli/src/paths.ts`, including application/cache paths. | Offline fixtures work without checkout `config.yaml`; missing required vault inputs fail clearly. No SAP call or credential symlink creation/deletion is needed. |
| 3 | Put the one preparation invocation into `.harness.json.bootstrap` and declare its package/lock/config inputs in `preparation.inputs`. Preserve the repository gate and extensions. | Changed declared inputs invalidate readiness; successful unchanged preparation is reused across both runtimes. String commands retain Bash semantics; argv commands select a verified executable. |
| 4 | Remove independent EnterWorktree setup registration as part of the config cutover, or use only the supported common transport. Remove path-name-based identity checks from the preparation body in favor of common Git inspection. | A new worktree and reentered existing worktree reach the same preparation contract once. Legacy registration is rejected, rather than silently running alongside common preparation. |
| 5 | Add a root `AGENTS.md` entry referencing the one repository policy source, currently under `.agents/`; align Claude entry pointers with that source. Choose one effective skill discovery path instead of adding duplicate bodies beside `.agents/skills.json` and `.claude/skills`. | Both runtime sessions actually load the intended rules/skills; duplicate registrations and absent root discovery fail verification. Static references alone do not prove loading. |
| 6 | Update `.claude/rules/harness-orchestration.md` to use `.harness.json`, common workspace commands and the existing ledger adapter. Replace direct bd operations and `.beads/redirect`-based harness detection while preserving repository remote-approval constraints. | Offline fixtures cover the selected backend without assuming beads; role separation, SIGNAL handling, human decisions and fresh review remain intact. |

Apply the preparation body and vault changes before switching the config/registration pair. Keep the prior repository revision and plugin/role artifacts for rollback, and test failure, retry, existing workspace entry and session resume before deployment. Follow the shipped [migration procedure](../plugins/harness/docs/migration.md) for artifact/config/state ownership; no skills installer edits these sap files.

An `npm ci` string selects the legacy Bash route. Changing it to `{"argv":["npm","ci"]}` does not establish native Windows support: environments where npm resolves to a `.cmd` launcher require an explicit adapter, and configured preparation separately requires the unsupported process-tree adapter. Choose an executable/transport only after verifying it on the intended host.

Native Windows package execution and preparation ownership require their own implementation/evidence. Claude live remains skills#268. Neither dependency is satisfied by historical Codex diagnostic snapshots or macOS fixture PASS. Release version and legacy-removal timing remain deployment decisions; retaining credential files or symlinks during rollback is intentional until a separate target-owned cleanup is reviewed.

Deployment decisions still required are the release version/compatibility scope, rollout schedule and legacy-removal date. Prepare those choices through the source repository's release procedure after the relevant evidence exists. The migration contract announces no release and does not activate the source artifact in any existing user session.
