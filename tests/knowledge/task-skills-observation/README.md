# 작업별 스킬 진입점 M0

실행 기준은 PR #410 head `ee03d6cd22ce019404aa7c8a9e055d3eebb26815`이다.
질문·기대 결과를 [m0.md](m0.md)에 먼저 고정한 뒤 서로 다른 독립 모델에 기존 계약과 후보 계약을 각각 읽혔다.
두 실행에는 정답표를 주지 않았고, 별도 평가자가 원본 응답과 고정 기준을 비교했다.
원장: skills#411 / skills#412 / skills#413.

- [질문](questions.json), [기존 응답](baseline-response.json), [후보 응답](candidate-response.json), [독립 판정](m0-evaluation.json)
- 양쪽 필수 경계 Q1–Q8 모두 통과. 후보는 여섯 책임별 진입점으로 직접 안내했다.
- 응답은 실제 모델 호출 결과다. 제품 명령을 실행한 기록이나 운영 권한 시스템 시험이 아니다.
- 호출 3건(독자 2건, 판정 1건)을 관측했다. 토큰·금액·지연은 실행 도구가 제공하지 않아 미측정이다.
- 응답 내 절대 경로는 당시 실험 좌표다. 현재 실행 경로로 사용하지 않는다.
- 실험 후보의 실행 계약은 이번 커밋의 여섯 SKILL 및 공통 참조와 같다. 위키 참조의 모호한 parent 포인터와 전환 이름은 구현 시 명시화했다.

제품 명령·로컬 fixture 회귀 결과와 최종 리뷰는 PR에서 별도 보고한다. 시험용 모델 응답을 쓰는 회귀 검사는 새 실제 모델 호출로 집계하지 않는다.
