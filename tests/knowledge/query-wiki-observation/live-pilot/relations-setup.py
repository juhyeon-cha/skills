from driver import *
host={'version':1,'repositories':{n:{'path':str(R/n),'url':'https://example.invalid/'+n,'scope':['app.py','check.py'],'project':str(R/('project-'+n))} for n in ['producer','consumer']},'principals':{p:{'manage':p=='operator','goals':['exchange'],'repositories':{n:(['source','query','publish'] if p!='restricted' or n=='producer' else []) for n in ['producer','consumer']}} for p in ['operator','reader','restricted']},'checks':{n+'-check':{'repositories':[n],'cwd_repository':n,'argv':[P,'-B','check.py']} for n in ['producer','consumer']}}
(R/'exchange-check.py').write_text('import importlib.util\nfrom pathlib import Path\nr=Path(__file__).parent\ndef load(n):\n    s=importlib.util.spec_from_file_location(n,r/n/"app.py");m=importlib.util.module_from_spec(s);s.loader.exec_module(m);return m\nassert load("consumer").consume(load("producer").produce()) == 1\n')
host['checks']['exchange-check']={'repositories':['producer','consumer'],'cwd_repository':'producer','argv':[P,'-B',str(R/'exchange-check.py')]};save('host.json',host)
save('goal-input.json',{'id':'exchange','version':'v1','status':'active','members':{n:{'target':'total 필드로 값 1을 교환한다. 누락 필드는 KeyError, 기본값/호환 변환 없음.','criteria':[n+'-check']} for n in ['producer','consumer']},'integration':['exchange-check'],'origin':{}})
