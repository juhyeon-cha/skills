생산코드 변경 전 입력·기대·판정 고정. 기존 Fixture의 로컬 producer/consumer와 synthetic 검토영수증만 사용. 실제 모델은 관측 판정을 별도로 담당.
Cases: complete→verified; document bytes drift→documents 단계 BINDINGS/CLAIM 내용 불일치; current same-source failed check→checks 단계 현재 입력 일치+exit !=0; failed check 후 command 변경→stale(현재 실패로 단정 금지); missing check→CHECK_PENDING; completed docs/checks but no review→REVIEW_PENDING; source dirty→SOURCE_DIRTY; missing document→documents unavailable; integration failure→모든member verified이나 통합 실패; revoke publish/query/source→그 member 진단도 비공개.
질문: 현재 막힌 단계는? 확인된 사실은? 다음 조치는? 숨긴 범위는? 정답은 직접 주입한 변경과 실제 내부 예외/check record 일치이며 fullrootcause아님.
PASS: 문서/검사/검토/소스의 첫 실패를 구분할 관측값 확보; freshfailure와stale 구분가능; 원본 예외/경로/출력은 공개안전 enum으로 변환가능; 기존 snapshot/hash계약은 read 외부에 보존가능. 불충분하면 unknown 사용. 비용은 실제 elapsed만, tokens/$ 미측정. 플랫폼/DB 도입 근거 없음.
