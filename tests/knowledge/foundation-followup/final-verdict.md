SIGNAL: MATCH

skills#339의 `27e1714..7535a8a`를 품질·수용 기준 함께 판정했다. 물리 worktree, linked=true, 브랜치 일치를 확인했다. 원장 조회는 `gh 인증이 없다`로 실패하여 전달된 AC 원문과 관측에 보존된 AC를 사용했다.

아래 `O`는 `tests/knowledge/foundation-followup/observation.json`, `T`는 `tests/knowledge/source-contract/test_project.py`다.

| ID | 수용 기준 인용 → 근거 → 판정 |
|---|---|
| S1-01-a | “개발 저장소 없이 복사 패키지에서 doctor→init→start 성공, 실제 의존성·입력 제약 기록. 의존성 누락은 변경 전 거절” → O.commands의 argv/rc, 설치 manifest, T:373 → MET |
| S1-01-b | “지원 호스트에서 작성·독립 리뷰·새 컨텍스트 인계 주체가 실제 필요한 도구를 실행. 파일 존재나 Python 자식 프로세스만으로 대체하지 않음” → O.roles의 작성자·리뷰어·handoff 실제 반환과 읽은 설치 경로 → MET |
| S1-02-a | “출발점·의도·권위·현재/목표 버전·AC·미반영 차이를 원문까지 추적. 누락·변조·잘못된 참조 거절” → O.inputs/classifier/분류 결과 대조, test_intake.py → MET |
| S1-02-b | “접수 원문 전체가 리뷰 패킷 ID에 결합. 코드·수정안은 같고 AC 또는 권위만 다르면 패킷 ID가 다르며 과거 리뷰 거절, 문서·기준 불변” → workflow.py:24,47, knowledge.py:93, T:321 직접 확인 → MET |
| S1-02-c | “과거 관측 원본은 그대로 재생. 접수 누락 과거 리뷰로 새 접수 검증을 통과시키지 않음” → M1 독립 실행 기록, v1/v2 경계와 T:321, 이번 변경에서 과거 원본 수정 없음 → MET |
| S1-03-a | “동일 프로젝트에서 코드 접수→작성→독립 반려→수정→새 리뷰→적용→다음 코드 변경 연결” → O의 initial/B/C 패킷·역할·명령·완료 연결 → MET |
| S1-03-b | “실제 분류에서 표현 변경은 의미 비교 후 코드 작업 불필요, 문서 동작 변경·사용자 목표는 목표·차이·AC를 가진 구현 대기. 분류만으로 문서·구현·배포 완료가 되지 않음” → 실제 classifier 응답과 네 CLI 결과 직접 일치 확인 → MET |
| S1-03-c | “이전 대화 없이 프로젝트 위치·실행 ID·설치 진입점만 받은 실행 주체가 미완료 실행을 이어 완료. 부모 대리 실행이나 이미 완료된 실행 조회는 대체 증거가 아님” → before-handoff=applying, fresh child의 직접 status/resume/status 반환 → MET |
| S1-04-a | “동일 요청은 동일 실행 반환. 경쟁 쓰기·다른 활성 요청은 문서·기준 변경 없이 거절” → T:77,138 및 변경 없는 M1 회귀 결과 → MET |
| S1-04-b | “context·packet·completion 손상 탐지 및 원본 보존. context의 잘못된 JSON·내용 변조·누락·심벌릭 링크 각각 복구 또는 종료 경로 확인” → knowledge.py:79, T:176,277,312와 M1 24경로 대조 → MET |
| S1-04-c | “적용 중 종료 후 같은 실행 재개 시 정확한 문서·완료 기록, 기준은 한 번 전진. 독립 편집은 보존하고 충돌 안내” → O의 rc91/applying/완료, 최종 실제 문서와 검토 after_text 바이트 일치 직접 확인; T:96,149 → MET |
| S1-04-d | “적용 전 종료·부분 적용 격리·범위 변경 후 새 기준 전환. 손상 DB 보존 및 새 프로젝트 안내, 격리 성공으로 허위 표시하지 않음” → T:176,219,228,312 및 기존 회귀·종료 경로 → MET |
| S1-05-a | “현재·목표·표현 문서의 권위·필수 명제·질문·입력 바이트를 관측 전에 고정” → O.document_fixture/freeze, 고정 prompt 해시 직접 확인 → MET |
| S1-05-b | “예외 누락·거짓 완료 문서는 대응 기준 실패, 수정본은 새 리뷰 통과. 변경 문서에 옛 응답 재사용 금지” → 서로 다른 normal/variant/repair 독자; CUR-2/TGT-2 실패와 복원본 5개 통과의 내용 직접 대조 → MET |
| S1-05-c | “코드 설명은 맞지만 접수 AC를 빠뜨린 수정안을 실제 독립 리뷰가 반려” → initial source_fidelity=pass, decision_coverage/uncertainty=fail, revise → MET |
| S1-06-a | “최종 소스와 설치 해시 일치. 같은 설치에서 설정·반려/수정·중단/재개·두 번째 변경 관측, 실제 읽기·응답·사용자 개입 보존” → 현재 소스·실설치·manifest 61개 해시와 모든 역할 prompt 해시 직접 일치 확인 → MET |
| S1-06-b | “같은 설치에서 코드·문서 표현·문서 동작·사용자 지시의 실제 분류 및 새 컨텍스트 인계. 준비된 JSON 수용 검사와 구분” → 원문 classifier 응답→접수 JSON 동등성 및 fresh handoff 확인 → MET |
| S1-06-c | “모든 조건의 현재 버전 판정·증거/미실행 사유가 독립 최종 검토와 일치. 필수 미충족이 남으면 전체 완료 보류” → 이번 독립 대조에서 18행의 근거·범위 일치, 필수 미충족 없음 → MET |

품질 MUST FIX/NIT와 미귀속 hunk 없음. 세 변경 파일 모두 실제 관측 보존과 기준별 판정에 귀속된다. 동일 독립 판정자의 복원본 재평가는 새 독자 응답을 사용했으며, 과거 실패를 취소하지 않는다. 독자 응답 복사본에는 마지막 줄바꿈만 추가되어 내용은 일치한다.

게이트·diff rc0은 심사 대상 `7535a8a` 자체의 기록을 적용했다. 기존 source-contract 52개·document-ac 7개·measurement 10개·foundation은 변경 없는 소스의 M1 독립 실행 결과이며 이번에 재실행하지 않았다. 전체 `tests/run-all.sh`도 미실행이다.

판정 범위는 합성 로컬 프로젝트와 부모가 관측한 실제 도구 반환, prompt-only 역할 분리다. provider 인증, native 접근 강제, marketplace 자동 로딩, 앱 재시작·새 최상위 작업, 다른 OS, 인간 사용성, 실제 서버·배포와 문서→코드 구현은 입증하지 않는다. 파일 수정·커밋·원장 쓰기는 수행하지 않았다.
