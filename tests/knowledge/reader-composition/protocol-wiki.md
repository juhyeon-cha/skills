# Explanation with rendered visual blocks — active observation

This supersedes the unexecuted author-only scope in `protocol.md` after the user
explicitly included actual wiki display. Preserve the earlier frozen proposal as
history; `frozen-wiki.json` identifies this observation's criteria and inputs.

Reuse the same synthetic cancellation source, role questions and existing twelve
answer criteria from `../reader-content/`. No new operational/business facts are
introduced. Apply C1-C3 and C5 from the earlier proposal. Replace C4 with: the guide
uses a flowchart to show the normal and uncertain-response branches, and a card group
for independently useful scope/decision facts; both render in static and managed wiki
using `[!FLOW]`/`[!CARDS]` safe Markdown lists. They retain useful linear reading order,
all conditions and unknowns, source links and searchable visible labels. Mermaid or
raw HTML is not part of the supported syntax. Titles alone do not establish visual success.

Author input: source, role questions, current writing-for-humans guidance and the
knowledge visual-block contract. Do not give the writer the old answer key or these
criteria. Output a Korean guide, evidence page and static wiki manifest. A fresh
reader gets only the guide and four questions: the problem and cancellation/refund
boundary; normal versus missing-response behavior and its reason; each role's
different next action; what the flow/cards explain and what remains uncertain.
An independent non-author judges the exact outputs and actual reader response against
the frozen source/criteria. Record real prompts and responses; do not claim a human study
or improvement over the previous version without a comparison.

Implementation checks exercise static and managed rendering, searchable branch/card
text, safe escaping and links, malformed-block errors and ordinary Markdown compatibility.
Build and inspect the real browser at wide and narrow widths. Confirm labels, connectors,
card reflow, heading links and reading order; record the inspected build and actual result.
Input/output hashes establish integrity only, not visual or semantic correctness.

Run repository gate and affected existing wiki/reader checks using a prepared local
Markdown runtime. No external downloads, publication or installed-copy update are needed.
Retain failures and repairs. Changes to evaluated document bytes require fresh affected
reader judgment. CSS-only layout fixes require renewed browser checks, not fabricated
new reader returns.
