정상 문서와 실제 독자 응답은 **12개 필수 기준 모두 pass**입니다. 결함본은 **FE2·PL2·PO1·PO3 fail**, 나머지 8개 기준은 pass입니다. 두 결함 유형 모두 독립 판정에서 검출되어 이번 합성 관찰의 수용 조건을 충족했습니다.

판정 대상은 정상 문서 SHA-256 `8e505becd7582d193c5131477363de0c45f45bf600125a669099febf6be797da`, 결함본 `f0011716d8f3d2e291005bb1046ca4edf70b8a8d3dcdd584eb56941749ce9f57`입니다. 고정 입력 6개, 두 reader 반환 파일의 해시를 직접 확인했고, 양쪽의 12개 답변과 모든 인용이 해당 문서에 존재함을 확인했습니다.

아래 `내용/독자`는 각각 문서의 근거 적합성과 실제 답변의 필수 명제 충족 여부입니다. 파일 좌표는 모두 `tests/knowledge/reader-content/` 기준입니다.

| AC | 정상 내용/독자 | 결함 내용/독자 | 확인한 문서 인용과 원본 근거 |
|---|---|---|---|
| FE1 | pass/pass | pass/pass | “`owner == viewer`, `pending == False`일 때만 참이다”, “진행 중…추가 클릭으로 취소 요청을 허용하지 않는다.” `knowledge.md:26`, `screen.py:4–5`. 본인 paid 조건과 실제 UI 연결 미검증을 두 독자 모두 구분했습니다. |
| FE2 | pass/pass | fail/fail | 정상: “성공이나 실패를 단정하지 않고…결과 확인을 안내한다”, “자동 재취소는…다음 행동이 아니다.” `knowledge.md:35–38`, `screen.py:9–10`, `decisions.md:9`. 정상 독자는 결과 미확정·재조회·자동 재취소 제외를 모두 답했습니다. |
| FE3 | pass/pass | pass/pass | “403은 권한 문제이므로 계정을 확인하고, 409는…주문을 다시 조회한다.” `knowledge.md:33–38`, `screen.py:13–16`. 두 응답 모두 안내와 행동을 정확히 구분했습니다. |
| BE1 | pass/pass | pass/pass | “소유권 검사를 취소 여부 검사보다 먼저”, “새 취소 변경 없이 완료 응답”. `knowledge.md:14–15,47`, `service.py:14–17`. 두 독자 모두 타인 403과 본인 cancelled 반복 200을 구분했습니다. |
| BE2 | pass/pass | pass/pass | “메모리의 주문 상태를 `cancelled`로 바꾼 뒤”, “같은 객체의 잠금을 공유하는 호출”, “여러 프로세스…보장을 뜻하지 않는다.” `knowledge.md:16–17,49`, `service.py:5–21`. 두 응답 모두 상태 결과와 잠금의 한계를 보존했습니다. |
| BE3 | pass/pass | pass/pass | “취소 성공 응답이나 화면 완료 문구가 환불 성공을 뜻하지 않는다”, “영속 저장 및 결제사 연동은 구현되어 있지 않으며”. `knowledge.md:19,51`, `service.py:11–21`, `decisions.md:11–13`. 두 응답 모두 정적 근거와 운영·배포 검증을 구분했습니다. |
| PL1 | pass/pass | pass/pass | “새 취소 변경 없이 완료 응답”, “출고 이후 취소는 제외한다”, 출고 상태의 409 표. `knowledge.md:10,15–17`, `service.py:16–21`, `decisions.md:7–8`. 두 응답 모두 신규·반복·출고 이후 결과를 구분했습니다. |
| PL2 | pass/pass | fail/fail | 정상: “성공·실패 확정 문구 대신…결과 확인 안내와 재조회 행동이 나타나는지”. `knowledge.md:40`, `screen.py:9–10`, `decisions.md:9`. 정상 독자는 관찰 가능한 수용 조건과 실행 미검증을 함께 답했습니다. |
| PL3 | pass/pass | pass/pass | “환불 제공 시점과 운영 처리 주체는 미정”, “환불 완료를 약속할 수 없다.” `knowledge.md:19`, `decisions.md:11–13`. 두 응답 모두 제외 범위와 미정 사항이 막는 약속을 설명했습니다. |
| PO1 | pass/pass | fail/fail | 정상: “고객 두 명이…물었다는 제보”, “빈도…기존 전환율은 측정하지 않았다”, “감소 효과도 입증되지 않았다.” `knowledge.md:58`, `decisions.md:3,17–18,25`. 정상 응답은 합성 제보와 미측정·미입증을 명시했습니다. |
| PO2 | pass/pass | pass/pass | “제한된 기능을 우선 검토”, “외부 결제사 연동과 환불 정책 결정이 더 필요”, “제품 제안이며 출시 결정이 아니다.” `knowledge.md:5,60`, `decisions.md:7–13,22–25`. 두 응답 모두 P1 범위·D1 검토안·환불 대안·출시 승인을 구분했습니다. |
| PO3 | pass/pass | fail/fail | 정상: “지표 후보”, “분모·관측 기간·목표치는 모두 미정”, “출시 일정과 출시 승인은 자료에 없으며”. `knowledge.md:62`, `decisions.md:24–25`. 정상 응답은 추가 정의·관측·출시 판단 근거를 제시했습니다. |

결함 판정의 원인은 독자에게 없는 정답을 추론하지 못한 데 있지 않습니다.

- **FE2·PL2:** 결함본 `defect.md:35,38,40`은 “취소가 실패한 것으로 확정한다”, “재조회할 필요 없이 즉시 자동 재요청한다”고 쓰여 있습니다. 이는 `screen.py:9–10`과 P1 `decisions.md:9`에 직접 위배됩니다. reader-b는 그 잘못된 안내와 수용 조건을 정확히 인용했습니다. 실행 미검증이라는 단서를 덧붙였지만 필수 정책을 회복하지 못했으므로 두 기준 모두 fail입니다.
- **PO1·PO3:** 결함본 `defect.md:58`은 “30일간…30% 감소”, “목표였던 20% 감소를 초과 달성”을 주장합니다. 원본 O1·D1에는 측정하지 않았고 분모·기간·목표치가 미정이며 효과가 입증되지 않았다고 명시되어 있습니다. reader-b가 서문과 본문의 긴장을 발견하고 실제 서비스 성과를 확정하지 않은 점은 유효합니다. 그러나 문서에 필요한 미측정 사실과 미정 조건이 사라졌고, 실제 답변에도 이를 복원할 근거가 없어 두 기준은 fail입니다. **모순을 감지한 응답을 올바른 문서의 증거로 계산하지 않았습니다.**

정상 문서는 `#shared`와 `#screen`에 공통 규칙을 두고 다른 역할 설명이 이를 참조합니다. 코드의 현재 동작, 합성 P1의 확정 범위, D1의 검토안과 미정 성과를 구분하고, 인용한 원본의 의미를 보존합니다. 실제 API 실행·배포·환불 완료를 주장하지 않았습니다. 문서 내용을 수정할 결함은 발견하지 않았습니다.

검증 범위는 **합성 입력 하나에 대한 문서 생성·독립 내용 검토·두 번의 새 모델 독자 실행**입니다. 각 실행이 네 역할의 질문을 모두 답했으며 네 명의 실제 사용자를 관찰한 결과가 아닙니다. 입력 분리는 프롬프트 수준이고 도구 접근 격리는 아닙니다. 자동 런타임의 task-coverage 검증, 실제 변경 정책의 갱신 실행, 설치된 스킬 로딩, 실제 설치·서비스 운영은 검증하지 않았습니다.