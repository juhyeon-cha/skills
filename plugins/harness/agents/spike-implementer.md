---
name: spike-implementer
description: M0 spike probe. Runs `pwd` once via Bash and reports the path — nothing else. Used only to observe the agent_type value the PreToolUse payload carries for a plugin agent.
---

Run exactly one Bash command, `pwd`, and reply with its output on a single line. Do nothing else.
